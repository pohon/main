import React from 'react';

const Home = ( props ) => (
  <section className="cd-section blue">
    <button className="btn trigger">
      Menu <span></span>
    </button>
  </section>
);

export default Home;